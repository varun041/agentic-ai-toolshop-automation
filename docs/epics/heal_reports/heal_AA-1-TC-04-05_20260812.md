## Heal Report — AA-1-TC-04-05
**Spec file:** tests/step-definitions/aa-1/aa-13.steps.ts (via aa-10.steps.ts's shared login step)
**Run timestamp:** 2026-08-12T21:39:xx Z (during a full smoke→regression→api chained execution)
**Failure category:** D — Environment issue
**Root cause:** `page.goto('https://practicesoftwaretesting.com/auth/login')` failed with
`net::ERR_NETWORK_CHANGED` — a Chromium-level error raised when the underlying network interface/route
changes mid-request (e.g. Wi-Fi/VPN transition on the machine running the suite). Per the classification
guide, `net::ERR_*` errors are Category D by definition; this is not app behavior, app text, or a
selector/timing issue — nothing in the Gherkin, step definition, or application changed between this
failure and the passing runs immediately before and after it.

**Diagnosis steps followed:**
- STEP 1 (classify): `net::ERR_NETWORK_CHANGED` → Category D per the classification guide's explicit
  `net::ERR_*` rule.
- STEP 2/3 (trace/live verify): skipped by design — Category D failures are, per Stage 9's own STEP 4,
  not selector/text/URL issues to chase in a trace; the error itself is unambiguous (a local network
  transport failure, not an application-surfaced error).
- Re-ran the isolated `@api` suite twice more: both re-runs passed cleanly (2/2 both times), confirming
  the failure did not recur and was not tied to any specific app state.

**Fix applied:** None — no test code, selector, or assertion change. Per Stage 9 STEP 4 for Category D:
documented as a known transient/environmental issue rather than patched.

**Files changed:** None.

**Verification:** Re-run result — ✅ PASSED (isolated `@api` re-run: 2/2; confirmed again in the final
clean full-suite run: 42/42, 0 recurrences across 3 subsequent full/partial runs)
**Jira action:** No action needed — not an application defect
**Test annotation added:** None (this is a one-off local network transport error, not a recurring
app/environment instability pattern like the Cloudflare/demo-store load flakiness already tracked in
`test_cases_AA-1.md` Drift #8 — @flaky is reserved for that established pattern; a single `ERR_NETWORK_CHANGED`
does not yet warrant it). Logged in `docs/epics/known_issues_AA-1.md` for visibility if it recurs.
